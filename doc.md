以下是复现 RMOEA/D 核心算法的最小框架文档，重点放在 MOEA/D + Q‑learning 参数自适应，并针对 Brandimarte 模糊柔性作业车间实例。

---

## 一、问题定义

### 1. 模糊柔性作业车间调度（MOFFJSP）
- 工件集合 \( J = \{J_1,\dots,J_n\} \)，机器集合 \( M = \{M_1,\dots,M_m\} \)。
- 每个工件 \( J_i \) 有一系列工序 \( O_{i,1}, O_{i,2}, \dots \)，每个工序可以在若干候选机器上加工。
- 加工时间为**三角模糊数** \( \tilde{p} = (t_1, t_2, t_3) \)，\( t_1 \) 最早，\( t_2 \) 最可能，\( t_3 \) 最晚。
- 待优化两个目标：
  1. 模糊最大完工时间 \( f_1 = \max \tilde{C}_{i,\text{last}} \) （模糊数）
  2. 总机器工作负载 \( f_2 = \sum_{k} \sum_{i,j} \tilde{p}_{i,j,k} \cdot x_{i,j,k} \) （模糊数）

模糊运算规则：
- 加法：分量分别相加。
- 排序（比较两个模糊数大小）：
  - 若 \( f_1 = (a_1+2a_2+a_3)/4 \) 不相等，按 \( f_1 \) 比较；
  - 否则按 \( f_2 = a_2 \) 比较；
  - 再否则按 \( f_3 = a_3-a_1 \) 比较。
- 取大：若 \( \tilde{a} > \tilde{b} \) 则 \( \max(\tilde{a},\tilde{b}) = \tilde{a} \)，否则为 \( \tilde{b} \)。

调度约束：
- 每台机器同时只能加工一个工序；
- 同一工件的工序必须顺序执行；
- 工序一旦开始不可中断。

---

## 二、数据集与模糊数生成

采用 **Brandimarte 柔性作业车间实例**（Mk01～Mk10），原始数据中每个工序的加工时间是一个确定值 \( b \)（清洗后）。

**转换为模糊数**（遵循论文）：
- 对每个确定时间 \( b \)，生成两个整数：
  - \( a \sim \text{randint}(0, \lfloor b/2 \rfloor) \)
  - \( c \sim \text{randint}(0, \lfloor b/2 \rfloor) \)
- 该工序的模糊加工时间定为 \( (a, b, c) \)。  
为了保证可复现，请固定随机种子（例如 `seed = 42`），并在代码中记录所使用的种子。

> 实例规模：Mk01(10×6)，Mk02(10×6)，Mk03(15×8)，Mk04(15×8)，Mk05(15×4)，Mk06(10×10)，Mk07(20×5)，Mk08(20×10)，Mk09(20×10)，Mk10(20×15)。

---

## 三、算法总体流程

### 1. 主要步骤（伪代码）

```
输入: 种群大小 Np, 最大迭代代数 Gen, 交叉率 R, 参数T候选列表 [5,10,15,20]
      Q-learning 参数 α=0.4, γ=0.6, ε=0.8, 记忆长度 LP=40
输出: 最终非支配解集 PF，及其 HV 值

1. 初始化权重向量 W (Np 个, 两目标, Das & Dennis 方法生成)
2. 初始化邻域 B(i) = 距离 W 最近的 T_init 个权重索引 (T_init 先用 10)
3. 初始化种群 P (使用 MIX3 规则, 见 3.3 节)
4. 初始化参考点 z* = (模糊无穷大, 模糊无穷大)
5. 初始化 Q-table: 4 状态 × 4 动作, 值全为 0
6. 计算当前 PF, 计算 CV 和 DV, 得到初始状态 S
7. For gen = 1 to Gen:
   a. 根据 Q-table 用 ε-greedy 选择动作 A (对应 T ∈ {5,10,15,20})
   b. 重新计算邻域 B(i): 为每个子问题选最近的 T 个邻居 (基于权重的欧氏距离)
   c. 执行 MOEA/D 一代更新:
      - 对每个子问题 i = 1..Np:
        - 从 B(i) 中随机选两个父代，用 POX + UX 交叉和变异产生子代 y
        - 修复 y 使其合法
        - 更新参考点 z*
        - 对每个邻居 j ∈ B(i)，若 y 的 Tchebycheff 值更好，则替换 P[j]
   d. 计算新一代 PF，更新精英存档 (可选, 见 3.6)
   e. 计算新状态的 CV, DV, 得到新状态 S'
   f. 计算奖励 R = 10 若 ΔDV>0 否则 0
   g. 更新 Q(S,A) = Q(S,A) + α [R + γ max_a Q(S',a) - Q(S,A)]
   h. S ← S'
8. 输出最终 PF 和 HV 值
```

---

### 2. 编码与解码

**编码**：每个解由两个等长向量表示，长度 = 总工序数。
- 操作序列向量（OS）：工序的加工顺序，元素为工件号（不直接是工序，需出现次数等于该工件工序数）。
- 机器分配向量（MA）：对应每个工序所选机器编号。

**解码**：
1. 将 OS 转换为具体的工序序列（工件号第几次出现就是第几道工序）。
2. 依次对每个工序，从其候选机器中取出 MA 指定的机器，获得模糊加工时间。
3. 按照工序序列，为每台机器维护一个**模糊时钟**（开始为空闲的零模糊数）。
   - 对于工序 \( O_{i,j} \)，其开始时间 = max( 同一工件前一工序完成时间 , 所选机器的空闲时间 ) （模糊 max）。
   - 完成时间 = 开始时间 + 加工时间（模糊加法）。
4. 所有工序完成后，记录两个模糊目标值：\( f_1 = \max_j \tilde{C}_{i,\text{last}} \)，\( f_2 = \sum_k \sum_{i,j} \tilde{p}_{i,j,k} \)（仅选取被实际使用的工序）。

---

### 3. 初始化策略 (MIX3 简化版)

生成 Np 个个体：
- 1/3 用 **Random**：随机打乱所有工序顺序，每个工序随机选一台候选机器。
- 1/3 用 **LS**（最小处理时间）：随机工序顺序，但每个工序固定选候选集里模糊数“最可能值” \( t_2 \) 最小的机器。
- 1/3 用 **GW**（全局最小工作负载）：
  - 工序顺序：先把所有工件的第一道工序随机排列，再接上剩余工序随机排列。
  - 机器选择：依次为每道工序选当前总工作负载（清晰值 \( (t_1+2t_2+t_3)/4 \)）增加最少的机器；平局时选 \( t_2 \) 最小的。
若总数不够 Np，剩余用 Random 补充。

---

### 4. MOEA/D 分解与更新

- **权重向量**：用 Das & Dennis 方法产生 Np = 100 个均匀分布在 2D 单纯形上的向量。
- **子问题聚合函数**：Tchebycheff 形式  
  \[
  g^{te}(x|\lambda, z) = \max_{k=1,2} \{ \lambda_k \cdot | f_k(x) - z_k | \}
  \]
  其中 \( f_k(x) \) 为目标值（解 x 的模糊 makespan 和总负载，需转换为**清晰值**参与计算，采用排序准则中的 \( f_1 \) 值：\( (t_1+2t_2+t_3)/4 \) 作为数值）。  
  参考点 \( z \) 每一代取当前种群中各目标（清晰值）的最小值。

- **交叉**：
  - 对操作序列：**POX**（基于工件顺序的交叉）。随机将工件分为两组，子代1从父1保留组1工件的位置，剩余空位按父2中这些工件的出现顺序填充。
  - 对机器分配：**UX**（均匀交叉）。生成随机二进制掩码，交换两个父代在掩码为1处的机器。
- **变异**：
  - OS变异：随机交换两个位置。
  - MA变异：随机选一个工序，将其机器改为候选集中的另一个随机可选机器。
- 所有交叉变异操作都要保证解合法。

---

### 5. Q‑learning 参数自适应 (Q-PAS)

这部分是论文的核心创新点，用于自动选择邻域大小 T。

**状态定义**（4种）：
每代结束后计算近似 Pareto 前沿的收敛性 CV 和多样性 DV 的变化量：
- 收敛性 \( CV = \frac{1}{|P|} \sqrt{ \sum_{y \in P} \min_{x \in P^*} \text{dis}(x,y)^2 } \)，其中 \( P \) 为当前前沿， \( P^* \) 取为理想点（(0,0)）， \( \text{dis} \) 为欧氏距离。
- 多样性 \( DV = \frac{1}{(N-1)\bar{d}} \sum_{i=1}^{N-1} |d_i - \bar{d}| \)，\( d_i \) 为前沿上相邻点欧氏距离，\( \bar{d} \) 为其均值。

根据 \( \Delta CV = CV_{t-1} - CV_t \) 和 \( \Delta DV = DV_t - DV_{t-1} \)：
- 状态1: ΔCV>0 且 ΔDV>0
- 状态2: ΔCV>0 且 ΔDV≤0
- 状态3: ΔCV≤0 且 ΔDV>0
- 状态4: ΔCV≤0 且 ΔDV≤0

**动作**：选择 T = 5, 10, 15, 20 中的一个。

**奖励**：
- 若 ΔDV > 0，奖励 = 10；
- 否则奖励 = 0。

**ε‑greedy 选择**：以概率 ε=0.8 随机选动作，以 1-ε 选当前状态 Q 值最大的动作（论文提到其 ε‑greedy 与传统相反，此处按描述实现即可，不影响效果）。

**Q 值更新公式**：
\[
Q(S,A) \leftarrow Q(S,A) + \alpha \big[ R + \gamma \max_{a} Q(S', a) - Q(S,A) \big]
\]
学习率 α=0.4，折扣因子 γ=0.6。

**应用方式**：每代开始前，用 Q-learning 选定一个 T，然后**整个种群统一使用这个 T** 来形成每个子问题的邻居列表 B(i)。

---

### 6. 精英存档（可选但推荐）

- 维护一个大小为 Np 的外部存档 A。
- 每代结束后，A = A ∪ 当前 PF，然后对 A 进行非支配排序，保留前 Np 个个体。
- 最终输出 PF 时使用存档 A 的内容。

---

## 四、参数总览

| 参数 | 值 |
|------|----|
| 种群大小 Np | 100 |
| 权重向量个数 | 100 |
| 邻域候选 T | [5,10,15,20] |
| 交叉率 | 0.8 (控制变异概率) |
| 最大代数 Gen | 200 |
| Q‑learning 学习率 α | 0.4 |
| 折扣因子 γ | 0.6 |
| ε (随机动作概率) | 0.8 |
| 记忆长度 LP | 40 (仅 RVNS 用，本简化版不涉及) |

---

## 五、评估与结果保存

### 1. 近似 PF 生成
最终输出存档 A 中的非支配解，每个解记录两个**清晰目标值**：
- makespan_clear = \( (t_{1}^{\text{makespan}} + 2t_{2}^{\text{makespan}} + t_{3}^{\text{makespan}})/4 \)
- workload_clear = \( (t_{1}^{\text{workload}} + 2t_{2}^{\text{workload}} + t_{3}^{\text{workload}})/4 \)

### 2. 性能指标
计算 **Hypervolume (HV)**：
- 所有实例共享参考点 \( r = (1.1, 1.1) \)（或根据实际前沿动态调整，推荐先对所有解归一化到 [0,1] 区间，再以 (1,1) 为参考点）。
- 计算最终存档中归一化解集的 HV 值。

### 3. JSON 输出格式
对每个实例（如 Mk01）独立运行一次（论文中是 30 次取平均，最小复现可只跑 1 次），保存结果到 `results.json`：

```json
{
  "Mk01": {
    "hv": 0.073,
    "pf": [
      [2.54, 15.3],
      [3.12, 13.8],
      ...
    ]
  },
  "Mk02": { ... },
  ...
}
```

若需要多次运行，可将每次的 HV 和 PF 存入列表。

---

## 六、实现注意事项

1. **模糊数运算**：所有模糊加法和取大操作都必须在调度仿真中正确实现。
2. **目标值清晰化**：仅在计算 Tchebycheff 值、CV、DV、HV 时，将模糊目标转换为排序值 \( f_1 \) 对应的数值。
3. **邻居列表**：随着 T 变化，每一代都要重新计算最近邻。
4. **交叉率**：对每一个子代，以概率 0.8 执行交叉，否则直接复制父代；变异总是在交叉后执行。
5. **子代修复**：交叉或变异可能导致非法解（如工序数不对），需要补齐或调整。

